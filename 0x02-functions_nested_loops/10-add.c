#include "main.h"

/**
 * int add - function that adds two integers and returns the result.
 * @j: first integer
 * @k: second integer
 * Return: sum of both
 */
int add(int j, int k)
{
	int s;

	s = j + k;
	return (s);
}
