#include "main.h"

/**
 * main - checks for lowercase character.
 * @c: character to be checked
 * Return: 1 (Success)
 */
int _islower(int c)
{
	if (c >=97 && 122)
	{
		return (1);
	}
	else
	{
		return (0);

	}
}
