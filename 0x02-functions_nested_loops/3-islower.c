#include "main.h"
#include <ctype>
/**
 * main - checks for lowercase character.
 *Return: 1 (Success)
 */
int main(void)
{
	int var = 'c';

	if (_islower(var))
	{	
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
