#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: character to be computed
 * Return: 1, 0 (Success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (1);
	}
	else if (n > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
