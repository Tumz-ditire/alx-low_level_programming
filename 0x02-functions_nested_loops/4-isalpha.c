#include "main.h"

/**
 * main - checks for alphabetic character.
 * @c: character to be checked
 * Return: 1 (Success)
 */
int main(void)
{
	char c;

	if (_isalpha(c) == 0)
	{
		return (0);
	}
	else
	{
		return (1);

	}
	
	return (0);
	
}
